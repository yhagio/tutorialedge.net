export class User {

    // profile settings which
    // link into email
    // and forum
    // course progress
    // 

    /*
     * posts returns all the published
     * posts by the current user
     */
    public posts(): any {
        return null;
    }

    /*
     * completedChallenges returns all the
     * challenges that a given user has completed
     */
    public completedChallenges(): any {
        return null;
    }

}